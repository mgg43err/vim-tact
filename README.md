# vim-tact
vim highlighting for tact language

create or update ```~/.vimrc``` from  ```.vimrc```

put ```syntax_tact.vim``` 
to  ```~/.vim/syntax/```

put ```ft_detect_tact.vim``` 
to  ```~/.vim/ftdetect/```
